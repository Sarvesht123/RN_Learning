import { ScrollView, StyleSheet, Text, View } from 'react-native'; import { SafeAreaView } from 'react-native-safe-area-context'; import { Header } from '@/components/Header'; import { colors } from '@/constants/colors'; import { fetchCmsPage } from '@/services/commerce'; import { useCommerce } from '@/hooks/use-commerce'; import { RequestState } from '@/components/RequestState';
const identifiers = ['about-us', 'delivery-information', 'privacy-policy-cookie-restriction-mode', 'terms-and-conditions'];
function CmsCard({ identifier }: { identifier: string }) { const result = useCommerce(() => fetchCmsPage(identifier), [identifier]); return <View style={styles.card}><RequestState loading={result.loading} error={result.error} retry={result.retry} />{result.data ? <><Text style={styles.title}>{result.data.heading || result.data.title}</Text><Text style={styles.copy}>{result.data.content}</Text></> : null}</View>; }
export default function Info() { return <SafeAreaView style={styles.safe}><ScrollView contentContainerStyle={styles.content}><Header title="Information" back />{identifiers.map((identifier) => <CmsCard key={identifier} identifier={identifier} />)}</ScrollView></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: colors.background }, content: { padding: 16 }, card: { backgroundColor: colors.white, borderRadius: 14, padding: 18, marginBottom: 12 }, title: { fontSize: 18, fontWeight: '900' }, copy: { color: colors.muted, lineHeight: 21, marginTop: 7 } });
